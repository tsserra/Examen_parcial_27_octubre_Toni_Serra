Recorre el listado del ejemplo y realiza las siguientes operaciones:

[18, 50, 210, 80, 145, 333, 70, 30]

Imprimir el número en caso de que sea múltiplo de 10 y menor que 200
Parar el programa si llega a un número mayor que 300
Organizar la lista mediante el método de ordenamiento merge sort
Dada la lista anterior y un valor 145 devolver el índice de 145 en la lista si 145 está en la lista, y −1 si 145 no está en la lista

lista_numeros = [18, 50, 210, 80, 145, 333, 70, 30]
def multiplos10_menor200():
    lista = list()
    for i in lista_numeros():
        if i // 10:
            if i <= 200:
                lista.append(i) 
            else:
                continue
        else:
            continue
return lista

def mayor300():
    for i in lista_numeros():
        if i > 300:
            break


def ordenar_list(lista_numeros, orden):
    def por_orden(orden):
        return orden[1]
lista1 = sorted(orden, key = por_orden, prioridad = True)
lista1.sorted(lista_numeros, orden)
orden = [1, 2, 3, 4, 5, 6, 7, 8]
lista1 = [18, 50, 210, 80, 145, 333, 70, 30]

def devolver(lista_numeros, numero):
    for i in lista_numeros:
        if valor in lista_numeros:
            lista_numeros.append(valor)
        else:
            lista_numeros.append(-1)
    return lista_numeros




